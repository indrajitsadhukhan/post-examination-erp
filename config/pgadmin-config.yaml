apiVersion: v1
kind: ConfigMap
metadata:
 name: pgadmin-config
data:
 servers.json: |
   {
       "Servers": {
         "1": {
           "Name": "PostgreSQL DB",
           "Group": "Servers",
           "Port": 5432,
           "Username": "postgres",
           "Host": "mypostgres",
           "SSLMode": "prefer",
           "MaintenanceDB": "postgres"
         }
       }
   }

---
apiVersion: v1
kind: Secret
metadata:
  name: pgadmin
  namespace: default
type: Opaque
stringData:
  pgadmin-password: test
